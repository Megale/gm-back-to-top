before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sudo apt-get install libfontconfig1
  - npm install -g karma-cli
  - npm install -g bower
  - bower install -f

language: node_js
node_js:
 - 0.8
